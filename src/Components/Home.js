import React from 'react';
import { View, StyleSheet } from 'react-native';

const Home = () => {
    return (
        <View>
            Home Page
        </View>
    );
}

const styles = StyleSheet.create({})

export default Home;
